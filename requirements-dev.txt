# Dev / CI dependencies layered on top of requirements.txt.
# Pinned to the same versions the GitHub Actions CI workflow uses so
# local `make qa` and CI stay in sync.

# --- Testing ---------------------------------------------------------------
-r requirements.txt
pytest==8.3.3
pytest-cov==5.0.0
pytest-asyncio==0.24.0
httpx==0.27.2

# --- PostgreSQL & Redis for integration tests -----------------------------
psycopg2-binary==2.9.9
redis==5.1.1

# --- Lint / format / type-check (also installed by pre-commit) -----------
black==24.10.0
isort==5.13.2
flake8==7.1.1
flake8-bugbear==24.10.31
flake8-comprehensions==3.16.0
mypy==1.13.0
types-requests==2.32.0.20240914

# --- Security tooling -----------------------------------------------------
bandit[toml]==1.7.10
pip-audit==2.7.3
safety==3.2.3

# --- Pre-commit / gitleaks wrappers --------------------------------------
pre-commit==4.0.1

# --- Backup / observability helpers --------------------------------------
boto3==1.35.45
prometheus-client==0.21.0
sentry-sdk[fastapi]==2.15.0
